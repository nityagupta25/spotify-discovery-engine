# Lean deps for RELIABLE Streamlit Community Cloud builds (no torch).
# The Off Repeat MVP (off_repeat_app.py) needs only streamlit + groq.
# The review-engine app (app.py) uses pandas/numpy/plotly/scikit-learn and
# gracefully falls back to TF-IDF keyword search when sentence-transformers
# is absent — so dropping the ~2GB torch dependency fixes the failing builds
# without breaking either app.
streamlit==1.50.0
pandas>=2.2.3
numpy>=2.1,<3
plotly>=6.0
scikit-learn>=1.5
groq>=0.11
